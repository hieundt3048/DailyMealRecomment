package com.example.btl.data.model

enum class Goal {
    GAIN_WEIGHT,    // Tăng cân
    LOSE_WEIGHT,    // Giảm cân
    MAINTAIN_WEIGHT // Giữ dáng
}

enum class DietType {
    VEGAN,          // Thuần chay
    NORMAL          // Bình thường
}